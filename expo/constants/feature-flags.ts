/**
 * 運営側専用の機能表示フラグ
 *
 * アプリ内にユーザーが操作できるUIは存在しない。
 * 切り替えは運営側がこのファイルを直接変更することでのみ行う。
 */

/**
 * マッチング機能（マッチングタブ・マッチング申請・スカウト申請）の表示フラグ
 * - true: マッチングタブ、スカウト申請の通知・一覧・承認/辞退UIを表示
 * - false: 上記UIをすべて非表示（データ・内部ロジックはそのまま維持）
 */
export const MATCHING_FEATURE_VISIBLE = false;
